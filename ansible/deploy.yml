---
- hosts: servers
  become: false

  tasks:

  - name: Pull image
    command: docker pull ayush81080/calculator:latest

  - name: Run container
    command: docker run -d --name calculator ayush81080/calculator:latest
